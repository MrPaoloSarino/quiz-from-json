# Handle npm bug with optional dependencies
legacy-peer-deps=true
# Allow optional dependencies (needed for esbuild / rollup platform binaries)
optional=true
# Do not forcibly omit optional deps; rely on npm's platform filtering
omit=