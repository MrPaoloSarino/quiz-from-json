# Handle npm bug with optional dependencies
legacy-peer-deps=true
# Ensure optional dependencies are NOT installed
optional=false
omit=optional
# Use npm's new resolution algorithm
resolution-mode=highest
# Exclude platform-specific packages
target_platform=linux
target_arch=x64 