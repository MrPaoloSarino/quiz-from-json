import type { FlashcardActivity } from "@/types/flashcard";

// Register new activity type: Flashcard
export const registerFlashcardActivity = (deck: FlashcardActivity) => {
  // Implementation for registering a flashcard deck
  // (This can be extended to support more activity types)
  // For now, just a placeholder
  // TODO: Integrate with persistent storage or global registry as needed
  return deck;
}; 